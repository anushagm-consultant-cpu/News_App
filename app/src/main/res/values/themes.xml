<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.News_App" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

    </style>
    <style name="Theme.AppCompat.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#ffffff</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>

        //tells the library which theme to use after the splash is done
        <item name="postSplashScreenTheme">@style/Base.Theme.News_App</item>


    </style>

    <style name="Theme.News_App" parent="Base.Theme.News_App" />
</resources>